{
	"name": "dream-expert-analysis",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"build": "cargo install -q worker-build && PATH=$HOME/.cargo/bin:$PATH worker-build --release"
	},
	"devDependencies": {
		"wrangler": "^4.24.4"
	}
}
